-- CREATE TABLE items (psql --version
--     id SERIAL PRIMARY KEY,  -- Automatically increments the ID for each item
--     name VARCHAR(100),      -- Item name
--     price DECIMAL(10, 2)    -- Price of the item with two decimal places
-- );


-- INSERT INTO items (name, price) VALUES
-- ('Small Desk', 100),
-- ('Large Desk', 300),
-- ('Fan', 80);


-- CREATE TABLE customers (
--     id SERIAL PRIMARY KEY,   -- Automatically increments the ID for each customer
--     firstname VARCHAR(100),  -- First name of the customer
--     lastname VARCHAR(100)    -- Last name of the customer
-- );


-- INSERT INTO customers (firstname, lastname) VALUES
-- ('Greg', 'Jones'),
-- ('Sandra', 'Jones'),
-- ('Scott', 'Scott'),
-- ('Trevor', 'Green'),
-- ('Melanie', 'Johnson');

-- SELECT * FROM items;

-- SELECT * FROM items WHERE price > 80;

-- SELECT * FROM items WHERE price <= 300;

-- SELECT * FROM customers WHERE lastname = 'Smith';

-- SELECT * FROM customers WHERE lastname = 'Jones';

-- SELECT * FROM customers WHERE firstname != 'Scott';










